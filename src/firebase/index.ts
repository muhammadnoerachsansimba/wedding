import { initializeApp } from "firebase/app";

export const firebaseApp = initializeApp({
	databaseURL:
		"https://wedding-7ea3c-default-rtdb.asia-southeast1.firebasedatabase.app.firebaseio.com",
});
